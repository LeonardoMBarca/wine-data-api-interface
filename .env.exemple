# CÓDIGO TEMPLATE DO .env PARA QUE TODOS TENHAM EM MENTE QUAIS VARIÁVEIS DE AMBIENTE CONFIGURAR PARA CONSEGUIR RODAR O CÓDIGO (Obviamente as variáveis de .env não devem ser carregadas para o GitHub por questões de segurança, então estão incluidas no .gitignore)